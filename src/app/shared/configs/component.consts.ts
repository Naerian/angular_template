import { ComponentColor, ComponentSize } from "./component.model";

export const DEFAULT_SIZE: ComponentSize = 'm'; // Tamaño por defecto para los componentes
export const DEFAULT_COLOR: ComponentColor = 'primary'; // Color por defecto para los componentes