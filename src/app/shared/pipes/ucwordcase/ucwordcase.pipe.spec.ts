import { UcwordcasePipe } from './ucwordcase.pipe';

describe('UcwordcasePipe', () => {
  it('create an instance', () => {
    const pipe = new UcwordcasePipe();
    expect(pipe).toBeTruthy();
  });
});
