/**
 * Define los tamaños preestablecidos para el spinner y su etiqueta.
 */
export type SpinnerSize = 'xs' | 's' | 'xm' | 'm' | 'l' | 'xl';

/**
 * Define la posición del spinner.
 * 'absolute' para superponerse a un contenedor, 'inline' para el flujo normal del documento.
 */
export type SpinnerPosition = 'absolute' | 'inline';