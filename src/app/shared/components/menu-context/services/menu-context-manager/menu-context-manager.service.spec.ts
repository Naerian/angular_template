import { MenuContextManagerService } from './menu-context-manager.service';

describe('MenuContextManagerService', () => {
  const service: MenuContextManagerService = new MenuContextManagerService();

  it('should create', () => {
    expect(service).toBeTruthy();
  });
});
