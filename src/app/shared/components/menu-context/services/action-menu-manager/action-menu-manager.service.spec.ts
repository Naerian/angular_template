import { ActionMenuManagerService } from './action-menu-manager.service';

describe('ActionMenuManagerService', () => {
  const service: ActionMenuManagerService = new ActionMenuManagerService();

  it('should create', () => {
    expect(service).toBeTruthy();
  });
});
