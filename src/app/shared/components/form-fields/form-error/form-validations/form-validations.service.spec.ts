import { FormValidationsService } from './form-validations.service';

describe('FormValidationsService', () => {
  const service: FormValidationsService = new FormValidationsService();

  it('should create', () => {
    expect(service).toBeTruthy();
  });
});
