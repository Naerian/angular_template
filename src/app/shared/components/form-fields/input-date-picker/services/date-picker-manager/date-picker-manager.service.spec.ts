import { DatePickerManagerService } from './date-picker-manager.service';

describe('DatePickerManagerService', () => {
  const service: DatePickerManagerService = new DatePickerManagerService();

  it('should create', () => {
    expect(service).toBeTruthy();
  });
});
