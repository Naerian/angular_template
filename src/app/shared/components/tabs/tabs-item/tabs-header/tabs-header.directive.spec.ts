import { TabsHeaderDirective } from './tabs-header.directive';

describe('TabsHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new TabsHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
